name: zmem
version: 0.8.6
description: "ZMem — local-first cross-session memory shared with ZCode, Claude Code, and Codex via a single ~/.zmem/store.sqlite. Passive recall, explicit memory tools, and Tier-0 core.md injection. Pair with the bundled shell hooks for the reflection loop."
